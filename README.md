w3c-validator-runner
====================

Python 3 script. Requires the Debian/Ubuntu package `w3c-markup-validator`. Also requires `python3-lxml` for full functionality. Does not require a webserver.
